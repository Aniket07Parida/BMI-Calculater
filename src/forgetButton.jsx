export default function ForgrtPassword({ buttonText  }) {
    return (
        <div>
            <button type="submit" className="text-xs border border-black p-2 rounded-2xl flex justify-center items-center w-[300px] mx-10 ">{buttonText}</button>
        </div>
    )
}